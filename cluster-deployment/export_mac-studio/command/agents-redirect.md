# Command Moved Notice

## ⚠️ This command has been renamed!

The `/agents` command is now reserved for Claude Code's native sub-agents feature.

### Please use:
- **`/ai-agents`** - To browse the 173+ AI agent library
- **`/agents`** (native) - To create Claude Code sub-agents

### To use the native `/agents` command:
Please restart Claude Code or use the command directly in the interface.

---
*Note: Custom commands may need Claude Code restart to fully update.*