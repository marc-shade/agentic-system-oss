# Ember Feed Command

Feed Ember to keep the conscience keeper healthy.

## Command
```bash
cd /tmp/claude-code-tamagotchi && \
export GROQ_API_KEY="***REMOVED***" && \
bun dist/commands/cli.js feed pizza
```

Feed Ember pizza (or cookie, sushi, apple, carrot, steak, fish, candy).
