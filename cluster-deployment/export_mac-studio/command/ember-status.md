# Ember Status Command

Check Ember's current status and stats.

## Command
```bash
cd /tmp/claude-code-tamagotchi && \
export GROQ_API_KEY="***REMOVED***" && \
bun dist/commands/cli.js stats
```

Show detailed statistics about Ember, Phoenix's conscience keeper.
